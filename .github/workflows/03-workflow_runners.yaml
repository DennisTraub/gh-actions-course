name: 03 - Workflow Runners

on: push

jobs:
  ubuntu-echo:
    runs-on: ubuntu-latest
    steps:
      - name: Show OS
        run: |
          echo "I'm running on an Ubuntu runner."
          echo "Runner OS: ${{ runner.os }} with ${RUNNER_OS}"